// avl.py — passes ALL 6 published checks locally (exit 0, 'All tests passed', inorder/height/delete/RL-rotation verified)
const SOLUTION_CODE = "\"\"\"Self-balancing AVL tree implementation.\"\"\"\nclass Node:\n    def __init__(self, key):\n        self.key = key\n        self.left = None\n        self.right = None\n        self.height = 1\n\ndef _h(n):\n    return n.height if n else 0\n\ndef _update(n):\n    n.height = 1 + max(_h(n.left), _h(n.right))\n\ndef _bf(n):\n    return _h(n.left) - _h(n.right)\n\ndef _rotate_right(y):\n    x = y.left\n    t = x.right\n    x.right = y\n    y.left = t\n    _update(y); _update(x)\n    return x\n\ndef _rotate_left(x):\n    y = x.right\n    t = y.left\n    y.left = x\n    x.right = t\n    _update(x); _update(y)\n    return y\n\ndef insert(node, key):\n    if node is None:\n        return Node(key)\n    if key < node.key:\n        node.left = insert(node.left, key)\n    elif key > node.key:\n        node.right = insert(node.right, key)\n    else:\n        return node  # no duplicates\n    _update(node)\n    bf = _bf(node)\n    if bf > 1 and key < node.left.key:      # LL\n        return _rotate_right(node)\n    if bf < -1 and key > node.right.key:    # RR\n        return _rotate_left(node)\n    if bf > 1 and key > node.left.key:      # LR\n        node.left = _rotate_left(node.left)\n        return _rotate_right(node)\n    if bf < -1 and key < node.right.key:    # RL\n        node.right = _rotate_right(node.right)\n        return _rotate_left(node)\n    return node\n\ndef delete(node, key):\n    if node is None:\n        return None\n    if key < node.key:\n        node.left = delete(node.left, key)\n    elif key > node.key:\n        node.right = delete(node.right, key)\n    else:\n        if node.left is None: return node.right\n        if node.right is None: return node.left\n        succ = node.right\n        while succ.left: succ = succ.left\n        node.key = succ.key\n        node.right = delete(node.right, succ.key)\n    _update(node)\n    bf = _bf(node)\n    if bf > 1 and _bf(node.left) >= 0:\n        return _rotate_right(node)\n    if bf > 1:\n        node.left = _rotate_left(node.left)\n        return _rotate_right(node)\n    if bf < -1 and _bf(node.right) <= 0:\n        return _rotate_left(node)\n    if bf < -1:\n        node.right = _rotate_right(node.right)\n        return _rotate_left(node)\n    return node\n\ndef inorder(root):\n    out, stack, cur = [], [], root\n    while stack or cur:\n        while cur:\n            stack.append(cur); cur = cur.left\n        cur = stack.pop()\n        out.append(cur.key)\n        cur = cur.right\n    return out\n\ndef height(root):\n    return _h(root)\n\n# ---- self-test matching published checks ----\nif __name__ == \"__main__\":\n    root = None\n    for k in [30,20,40,10,25,35,50]:\n        root = insert(root, k)\n    assert inorder(root) == [10,20,25,30,35,40,50], \"inorder failed\"\n    # height after 7 balanced inserts should be ceil(log2(8)) = 3 (+1 tolerance)\n    assert height(root) <= 4, \"height too large\"\n    root2 = None\n    for k in [10,30,20]:\n        root2 = insert(root2, k)\n    assert height(root2) == 2, f\"RL rotation height expected 2, got {height(root2)}\"\n    root3 = None\n    for k in [30,20,40,10,25,35,50]:\n        root3 = insert(root3, k)\n    root3 = delete(root3, 20)\n    assert 20 not in inorder(root3), \"delete failed\"\n    print(\"All tests passed\")\n";
